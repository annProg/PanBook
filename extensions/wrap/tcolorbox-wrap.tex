\usepackage[most]{tcolorbox}
\usepackage{fontawesome5}
\newtcolorbox{info}[1][\infoTitle]{%
  enhanced jigsaw, % better frame drawing
  borderline west={2pt}{0pt}{infoBorder}, % straight vertical line at the left edge
  %sharp corners, % No rounded corners
  boxrule=0pt, % no real frame,
  fonttitle={\large\bfseries},
  coltitle={black},  % Black colour for title
  colbacktitle=info,
  arc=4pt,
  title={\infoTitleFa#1~~},  % Fixed title
  attach title, % Move the title into the box
}

\newtcolorbox{warn}[1][\warnTitle]{%
  enhanced jigsaw, % better frame drawing
  borderline west={2pt}{0pt}{warnBorder}, % straight vertical line at the left edge
  %sharp corners, % No rounded corners
  boxrule=0pt, % no real frame,
  fonttitle={\large\bfseries},
  coltitle={black},  % Black colour for title
  colbacktitle=warn,
  arc=4pt,
  title={\warnTitleFa#1~~},  % Fixed title
  attach title, % Move the title into the box
}

\newtcolorbox{alert}[1][\alertTitle]{%
  enhanced jigsaw, % better frame drawing
  borderline west={2pt}{0pt}{alertBorder}, % straight vertical line at the left edge
  %sharp corners, % No rounded corners
  boxrule=0pt, % no real frame,
  fonttitle={\large\bfseries},
  coltitle={black},  % Black colour for title
  colbacktitle=alert,
  arc=4pt,
  title={\alertTitleFa#1~~},  % Fixed title
  attach title, % Move the title into the box
}

\newtcolorbox{tip}[1][\tipTitle]{%
  enhanced jigsaw, % better frame drawing
  borderline west={2pt}{0pt}{tipBorder}, % straight vertical line at the left edge
  %sharp corners, % No rounded corners
  boxrule=0pt, % no real frame,
  fonttitle={\large\bfseries},
  coltitle={black},  % Black colour for title
  colbacktitle=tip,
  arc=4pt,
  title={\tipTitleFa#1~~},  % Fixed title
  attach title, % Move the title into the box
}

\newtcolorbox{help}[1][\helpTitle]{%
  enhanced jigsaw, % better frame drawing
  borderline west={2pt}{0pt}{helpBorder}, % straight vertical line at the left edge
  %sharp corners, % No rounded corners
  boxrule=0pt, % no real frame,
  fonttitle={\large\bfseries},
  coltitle={black},  % Black colour for title
  colbacktitle=help,
  arc=4pt,
  title={\helpTitleFa#1~~},  % Fixed title
  attach title, % Move the title into the box
}
